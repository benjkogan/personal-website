"use client";

export default function Cursor() {
  // The custom cursor is now handled entirely by CSS (cursor-dot.png).
  // This component is kept as a mount point in case we need a JS fallback.
  return null;
}
